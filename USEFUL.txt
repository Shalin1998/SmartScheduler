<table class="tournament-table" style="overflow:hidden;width:100%;height:500px;overflow:hidden;">
  <% @game.each do |g| %>
  <tr class="tournament-row" style="text-align:center;padding:5px;">
  <%= Tournament.find(g.tournament_id).name %>
  <br>
  <%= Team.find(g.team_one).name %> vs. <%= Team.find(g.team_two).name %>
  <br>
  <%= g.date_time.strftime("%a. %B %d, %Y @ %I:%M%p") %>
  <br>
  Location: <%= Field.find(g.field_id).name %>
  </tr>
  <hr>
  <br>
  <% end %>
</table>
